<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Rails WebSockets on Lambda</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", media: 'all', 'data-turbolinks-track': 'reload' %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
    <%= action_cable_meta_tag %>
    <%# hotwire_livereload_tags if Rails.env.development? %>
  </head>
  <body>
    <nav class="navbar navbar-expand bg-body-secondary">
      <div class="container-fluid">
        <a class="navbar-brand p-0 m-0" href="/">
          <img src="https://lamby.cloud/img/lamby-logo-small.png" alt="Lamby Logo" height="30" class="d-inline-block align-text-middle m-0 mb-2">
          <span class="fs-6">Rails WebSockets on Lambda</span>
        </a>
        <ul class="navbar-nav">
          <% if current_user %>
            <li class="nav-item">
              <span class="nav-link">👤 <%= current_user.name %></span>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/rooms">🏠 Rooms</a>
            </li>
          <% end %>
          <li class="nav-item">
            <a class="nav-link" href="https://lamby.cloud">📑 Docs</a>
          </li>
          <% if current_user %>
            <li class="nav-item">
              <%= button_to '👋 Logout', logout_path, method: :delete, class: 'nav-link',
                  data: { controller: 'user', action: 'click->user#logout', turbo_method: :delete, turbo_frame: '_top' } %>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>
    <div class="p-3">
      <% if notice %>
        <div id="notice" class="alert alert-info py-2 mb-2" role="alert">
          💁‍♀️ <%= notice %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
